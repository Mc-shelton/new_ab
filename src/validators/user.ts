import { IuserValidator } from "./validator.types"

class userValidator implements IuserValidator {
    private user:string
    constructor(user:string){
        this.user = user
    }
}



()=>{
    return new Promise<string>((resolve, reject) => {
        
    })
}